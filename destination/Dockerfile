FROM openjdk:11.0.1-jre-slim-stretch
ARG APPLICATION_NAME
ARG APPLICATION_VERSION
ENV APPLICATION_NAME=${APPLICATION_NAME}
ENV APPLICATION_VERSION=${APPLICATION_VERSION}
ENV JAVA_OPTS -Xmx90m -Xms90m -XX:MaxMetaspaceSize=80m
COPY target/${APPLICATION_NAME}-${APPLICATION_VERSION}.jar ${APPLICATION_NAME}-${APPLICATION_VERSION}.jar
EXPOSE 80
CMD java ${JAVA_OPTS} -Dspring.config.location=file:/application.yml -Dlogger.encode=true -Dlogging.config=/logback.xml -jar ${APPLICATION_NAME}-${APPLICATION_VERSION}.jar
